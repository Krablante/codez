# Sample configuration

Codez inherits upstream Codex configuration behavior. Use the upstream docs for
baseline config examples, and use the Codez docs for fork-specific runtime, App
Server v2, and plugin behavior.

For a sample configuration file, see [this documentation](https://developers.openai.com/codex/config-sample).
