# Non-interactive mode

Codez keeps upstream `codex exec` compatibility for CLI workflows. Gateway
integrations that need the full Codez stack should use App Server v2 instead;
`codex exec --json` remains a compatibility path.

For information about non-interactive mode, see [this documentation](https://developers.openai.com/codex/noninteractive).
